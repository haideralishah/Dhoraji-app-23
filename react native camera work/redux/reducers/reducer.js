
const INITIAL_STATES = {
    username: 'Aliyar',
    auth: false,
    friendList: [],
    chatThread: [],
    
}

export default function (state = INITIAL_STATES, action) {

    switch (action.type) {
        default:
            return state;
    }

}